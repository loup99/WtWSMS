decisions = {
	PATRONS_ROOT_WEST = {
		potential = {
			is_playable = yes
			is_landed = yes
			age = 16
			not = { 
				trait = incapable
				trait = infirm
				trait = possessed 
			}
			NOT = {
				has_character_modifier = loving_them_monkeys
				has_character_modifier = loving_dat_crane
			}
			OR = {
				religion = root_west_pagan
				religion = root_west_pagan_reformed
			}
		}
		effect = {
			character_event = { id = PATRONS_ROOT_WEST.1 }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.2
				trait = slothful
			}
			modifier = {
				factor = 0.05
				trait = cynical
			}
		}
	}
}